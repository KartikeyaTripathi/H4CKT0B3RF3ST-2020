#include<bits/stdc++.h>
using namespace std;
int main()
{
   ios::sync_with_stdio(0);
   cin.tie(0);
   int t;
   cin>>t;
   string str;
   while(t--)
   {
     cin>>str;
     reverse(str.begin(),str.end());
     cout<<str<<"\n";
   }
   
   return 0;
}
  
