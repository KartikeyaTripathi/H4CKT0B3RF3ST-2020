#include<bits/stdc++.h>
using namespace std;
int main()
{
  ios_base::sync_with_stdio(false);
 
	int t;
  cin>>t;
  while(t--)
  {
    long long int n,count=0;
    cin>>n;
    while(n){
     ++count;
     n=n&(n-1);
    }
    cout<<count<<"\n";
  }
  return 0;
}
