#include <bits/stdc++.h>
using namespace std;
int main()
{
   ios_base::sync_with_stdio (false);
   unsigned long long int num;
   while(cin>>num 
   {
    int count=0;
    while(num)
    {
      num=num & (num-1);
      count++; 
    }
    cout<<count<<endl;
   }
   return 0;
}
